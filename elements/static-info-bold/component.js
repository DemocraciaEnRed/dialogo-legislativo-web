import styled from 'styled-components'

const StaticInfoBold = styled.p`
  font-size: 2.4rem;
  line-height: 3.0rem;
  max-width: 850px;
  color: var(--black);
  font-weight: bold;
  margin-top:3rem;
  margin-bottom: 3rem;
  text-align: justify;
  text-justify: inter-word;
`

export default StaticInfoBold
