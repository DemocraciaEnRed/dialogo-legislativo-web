import styled from 'styled-components'

const StaticInfoBold = styled.span`
  font-size: 2.4rem;
  line-height: 3.0rem;
  color: var(--black);
  font-weight: bold;
  margin-bottom: 40px;
`

export default StaticInfoBold
